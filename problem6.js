// Write a function that takes an array of strings and returns a new array with only the 
// unique strings. Use the Map object to accomplish this.
function removeAnDuplicate (arr){
    remove=strings.map((e)=>{
        if(!(newString.includes(e)))
          newString.push(e)
        
      })
      return newString
}
strings=['roshin','gokul','selva','jagan','roshin','gokul','singaram']
newString=[]
console.log(removeAnDuplicate(strings))